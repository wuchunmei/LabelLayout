<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:orientation="vertical"
    >

    <View
    android:id="@+id/tabs_div"
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:background="@drawable/list_divider" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imageViewIcon"
            android:layout_width="30dp"
            android:layout_height="20dp"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="8dp" />

        <TextView
            android:id="@+id/msgcount"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:translationX="-10dp"
            android:translationY="-4dp"
            android:layout_alignTop="@id/imageViewIcon"
            android:layout_toRightOf="@id/imageViewIcon"
            android:background="@drawable/shape_redpoint"
            android:gravity="center"
            android:text="99"
            android:textColor="@color/white"
            android:textSize="10dp"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivredp"
            android:layout_width="@dimen/notify_read_point_size"
            android:layout_height="@dimen/notify_read_point_size"
            android:translationX="-8dp"
            android:translationY="-4dp"
            android:layout_alignTop="@id/imageViewIcon"
            android:layout_toRightOf="@id/imageViewIcon"
            android:src="@drawable/shape_redpoint"
            android:visibility="gone" />
    </RelativeLayout>

    <TextView
        android:id="@+id/textViewLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:text="@string/tab_title_home"
        android:textColor="@color/tab_label_inactive_color"
        android:textSize="10dp" />

</LinearLayout>